#!/bin/bash -e

if [ -f migrate.sh ];
    sh ./migrate.sh
fi

echo start run -----------------------

if [ -x ${STI_SCRIPTS_PATH}/run ]; then
    source ${STI_SCRIPTS_PATH}/run $@
fi

echo end run -----------------------
