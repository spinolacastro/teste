#!/bin/bash
echo start run -----------------------

    /tmp/stunnel /tmp/stunnel.conf
if [ -x ${STI_SCRIPTS_PATH}/run ]; then
    source ${STI_SCRIPTS_PATH}/run
fi
echo end run -----------------------
